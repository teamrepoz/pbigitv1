{
  "fixes": [
    {
      "type": "measure",
      "table": "accounts+",
      "name": "Active Accounts Count",
      "dax_expression": "DISTINCTCOUNT('accounts+'[AccountID])",
      "solution_explanation": "Converted Tableau COUNTD([AccountID]) to DAX DISTINCTCOUNT('accounts+'[AccountID]). COUNTD is a distinct-count aggregation, so this is implemented as a measure using DISTINCTCOUNT on the AccountID column.",
      "type_changed_reason": "This calculation uses aggregation (COUNTD), so it should be created as a measure."
    }
  ]
}