{
  "fixes": [
    {
      "type": "calculated_column",
      "table": "accounts+",
      "name": "Withdrawal Amount",
      "dax_expression": "IF('accounts+'[TransactionType] = \"Withdrawal\", 'accounts+'[Amount], BLANK())",
      "solution_explanation": "Converted Tableau IF to DAX IF and mapped column references to 'accounts+'[Column]. Returns Amount when TransactionType = \"Withdrawal\", otherwise BLANK(). This is a row-level transformation so it is a calculated column."
    }
  ]
}