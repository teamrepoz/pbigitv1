{
  "fixes": [
    {
      "type": "calculated_column",
      "table": "accounts+",
      "name": "Deposit amount",
      "dax_expression": "IF('accounts+'[TransactionType] = \"Deposit\", 'accounts+'[Amount])",
      "solution_explanation": "Converted Tableau IF ... THEN ... END to DAX IF and mapped columns to 'accounts+'[ColumnName]. This is row-level logic (no aggregation), so it is implemented as a calculated column."
    }
  ]
}