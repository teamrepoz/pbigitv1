{
  "fixes": [
    {
      "type": "calculated_column",
      "table": "Risk_GFIRates_Reports_Excel_Rolling (local copy)",
      "name": "Sheet1 - Value",
      "dax_expression": "INT(BLANK())",
      "solution_explanation": "Tableau INT(NULL) yields a null integer. In DAX BLANK() represents null; using INT(BLANK()) preserves the integer cast intent. This is a row-level transformation with no aggregation, so it is implemented as a calculated column."
    }
  ]
}