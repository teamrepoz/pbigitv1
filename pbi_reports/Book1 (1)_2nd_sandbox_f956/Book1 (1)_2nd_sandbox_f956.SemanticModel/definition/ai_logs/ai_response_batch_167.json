{
  "fixes": [
    {
      "type": "calculated_column",
      "table": "Risk_GFIRates_Reports_Excel_Rolling (local copy)",
      "name": "Sheet1 - Modified By",
      "dax_expression": "BLANK()",
      "solution_explanation": "Tableau STR(NULL) produces a null string. In DAX the equivalent null value is BLANK(), so the calculated column returns BLANK(). This is a row-level operation (no aggregations or parameters), so it is implemented as a calculated column."
    }
  ]
}