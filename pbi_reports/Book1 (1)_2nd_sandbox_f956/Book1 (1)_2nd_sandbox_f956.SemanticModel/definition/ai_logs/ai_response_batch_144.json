{
  "fixes": [
    {
      "type": "measure",
      "table": "Risk_GFIRates_Reports_Excel_Rolling (local copy)",
      "name": "Book Count Filter",
      "dax_expression": "VAR lodCount = CALCULATE(\n    DISTINCTCOUNT('Risk_GFIRates_Reports_Excel_Rolling (local copy)'[Book Level 1]),\n    ALL(\n        'Risk_GFIRates_Reports_Excel_Rolling (local copy)'[Book Level 1],\n        'Risk_GFIRates_Reports_Excel_Rolling (local copy)'[TERM_LABEL]\n    )\n)\nRETURN IF(lodCount > 6, \"Dynamic Height\", \"Dynamic View\")",
      "solution_explanation": "Recreated the Tableau LOD + COUNTD by using CALCULATE with DISTINCTCOUNT and ALL to remove filters on [Book Level 1] and [TERM_LABEL], then applied the IF test to return the two string outcomes.",
      "type_changed_reason": "The calculation uses aggregation (COUNTD/DISTINCTCOUNT) and a Tableau LOD ({EXCLUDE ...}), which are context/aggregate-based operations — per the rules it must be implemented as a measure."
    }
  ]
}