{
  "fixes": [
    {
      "type": "calculated_column",
      "table": "HR Dataset",
      "name": "Calculation4",
      "dax_expression": "INT(BLANK())",
      "solution_explanation": "Tableau INT(NULL) is converted to DAX INT(BLANK()). This is a row-level type conversion without aggregation, so it is implemented as a calculated column."
    }
  ]
}