{
  "fixes": [
    {
      "type": "calculated_column",
      "table": "HR Dataset",
      "name": "Calculation4",
      "dax_expression": "INT(BLANK())",
      "solution_explanation": "Tableau INT(NULL) returns null; in DAX the equivalent is INT(BLANK()). This is a row-level conversion (no aggregation or parameters), so it should be implemented as a calculated column."
    }
  ]
}