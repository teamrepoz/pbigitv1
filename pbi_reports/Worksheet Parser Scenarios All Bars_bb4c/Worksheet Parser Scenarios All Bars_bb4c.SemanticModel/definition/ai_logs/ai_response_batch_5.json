{
  "fixes": [
    {
      "type": "calculated_column",
      "table": "HR Dataset",
      "name": "Calculation4",
      "dax_expression": "INT(BLANK())",
      "solution_explanation": "Translated Tableau INT(NULL) to DAX INT(BLANK()). In DAX, NULL is represented as BLANK(); INT applied to BLANK() yields BLANK(). This is a row-level transformation without aggregation, so it is implemented as a calculated column."
    }
  ]
}