{
  "fixes": [
    {
      "type": "calculated_column",
      "table": "order_items+",
      "name": "Days to Ship",
      "dax_expression": "DATEDIFF('order_items+'[OrderDate], 'order_items+'[ShipDate], DAY)",
      "solution_explanation": "Translated Tableau DATEDIFF('day', [OrderDate], [ShipDate]) to DAX DATEDIFF(start_date, end_date, DAY). This is a row-level date calculation (no aggregation or parameters), so it is a calculated column."
    }
  ]
}