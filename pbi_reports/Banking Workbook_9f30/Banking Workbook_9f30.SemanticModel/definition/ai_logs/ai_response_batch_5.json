{
  "fixes": [
    {
      "type": "calculated_column",
      "table": "accounts+",
      "name": "Withdrawal Amount",
      "dax_expression": "IF('accounts+'[TransactionType] = \"Withdrawal\", 'accounts+'[Amount], BLANK())",
      "solution_explanation": "This is a row-level conditional that returns the row's Amount when TransactionType = \"Withdrawal\". It uses no aggregates or parameters, so it is implemented as a calculated column. Column references were converted to the 'table'[column] format."
    }
  ]
}