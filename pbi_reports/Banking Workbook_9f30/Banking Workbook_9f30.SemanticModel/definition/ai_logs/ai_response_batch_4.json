{
  "fixes": [
    {
      "type": "calculated_column",
      "table": "accounts+",
      "name": "Deposit amount",
      "dax_expression": "IF('accounts+'[TransactionType] = \"Deposit\", 'accounts+'[Amount])",
      "solution_explanation": "Converted the Tableau IF row-level expression to a DAX calculated column using IF(). All column references were transformed to the 'TableName'[Column] format. The expression returns 'accounts+'[Amount] when TransactionType = \"Deposit\" and BLANK() otherwise."
    }
  ]
}